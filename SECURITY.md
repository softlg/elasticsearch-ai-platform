# 安全策略

## 支持版本

目前仅对默认分支（`main`）和最新发布版本提供安全修复。

## 报告漏洞

请不要在公开 Issue 中发布可复现的漏洞、密钥或日志样本。请通过 GitHub Security Advisories（首选）或仓库维护者的私密联系方式报告，并提供：

- 受影响的版本和部署方式
- 漏洞影响和复现步骤
- 必要的最小化日志或请求示例（脱敏后）
- 可行的修复建议（如有）

API Key、密码等凭据一旦泄露，应立即在对应供应商处撤销和轮换。

维护者主页：<https://github.com/softlg>。仓库开启 Security Advisories 后，请优先使用该私密渠道。

## 部署安全基线

- 不要把 `.env`、真实日志或生产数据提交到仓库。
- 生产环境应关闭 `CORS_ORIGINS=*`，并在反向代理或应用层增加认证、授权和限流。
- 发送给大模型前应按组织要求脱敏日志中的个人信息、令牌和业务机密。
- Elasticsearch 使用 HTTPS 和最小权限的只读账号。
